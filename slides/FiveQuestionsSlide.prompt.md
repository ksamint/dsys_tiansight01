Numbered step row — default content is the four stages 开店之前 → 复制扩张, one stage filled bronze. Columns follow the step count, so the older 经营五问 loop still renders when its five steps are passed. Also exported as `StagesSlide`.

```jsx
<FiveQuestionsSlide active={1} page={4} />
```
